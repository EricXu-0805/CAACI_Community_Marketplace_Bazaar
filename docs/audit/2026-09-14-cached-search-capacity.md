# 搜索缓存与发布审核性能 — 2026-09-14

基线 main `e1f3f0162c0cb3f38515874768727477e112ebc1`。本轮目标是继续关闭已知复杂搜索容量问题，同时保留原有结果、权限和审核规则。本报告仍在验收中；两项候选只应用于测试环境，尚未发布生产。

## 已验证的候选

`20260914214912_cached_listing_search.sql` 将商品的原生 pg_trgm 字符片段提前计算，并保存在未暴露的 `search_private.item_fields`。新发布、改标题/描述、翻译更新和路线修改由同一事务的触发器更新；删除由外键级联清理。价格、浏览数等变化不重复计算文本。缓存读取仍通过父商品的 RLS，搜索 RPC 保持 SECURITY INVOKER。

评分使用原生 `show_trgm` 的 24 位值，包括多语言字符的原生哈希碰撞；用整数集合交集计算相同的 float4 相似度。高分标题已足以确定结果时，仅按数学上限跳过不可能改变排名的字段。没有缩短描述、删搜索词、降低匹配阈值或提前截断候选。

`current_setting` 会舍入浮点设置的文本表示，所以实现使用原生 `show_limit()` 的 float4 值；恰好相等时交回原生 `%` 判断，保留底层更高精度的阈值语义。空字段的通配符得分、译文/路线重复字段在 v1/v2 的不同权重均有原 SQL 对照。

`20260914220849_inline_moderation_han_fold.sql` 只优化纯文本繁简转换。完整常量映射不变，调用明确限定 `pg_catalog.translate`，去掉阻止优化的函数配置切换。审核词、意图规则、审核结果、函数执行权限不变；没有缓存审核结论。

## 本地证据

真实 PostgreSQL 17 分别使用 libc 和 ICU `en-US`，原部署查询为对照，验证完整有序 JSON 结果与得分：

- 匿名、普通用户、被停权本人，隐藏/售出/删除商品和实时停权变化。
- 中文、拉丁特殊字符、emoji、百分号、下划线、反斜线，12 个词、空参数、非法数组、非默认数组下标。
- 多个浮点阈值边界，分类/价格/地点/日期/价格单位筛选与分页。
- 发布、改名、翻译、路线、下架、删除、回滚；两个并发编辑，以及发布与迁移回填竞争。
- 私有缓存不能由客户端写入；维护函数不能被客户端调用；完整回滚和重新应用保持相同搜索结果。

一万零一条商品、三次计时中位数，包含本机数据库客户端启动开销；**不是托管 HTTP p95**：

| 语料 / 查询 | 原查询 | 完整缓存迁移 |
|---|---:|---:|
| 重复描述 / 单词 | 158ms | 82ms |
| 重复描述 / 三词 | 449ms | 102ms |
| 重复描述 / 12 词 | 2,275ms | 116ms |
| 每条独特长描述 / 单词 | 524ms | 103ms |
| 每条独特长描述 / 三词 | 1,288ms | 130ms |
| 每条独特长描述 / 12 词 | 5,474ms | 137ms |

两类语料中宽泛搜索和无结果搜索也保持完整结果一致。缓存约 20.8MB；本机回填分别约 1.3 / 2.2 秒。测试集不能代表百万商品规模或持续生产负载。

审核验证覆盖现有广告/正常二手交易语料、全部非零且非代理项 BMP 字符和补充字符样本、恶意函数查找路径、词表即时修改和回滚。本地全套检查为 1,395 passed / 0 failed / 1 skipped；跳过项是本机缺失的 Vercel 产物清单，不能算成已测试。

## 托管进度与已发现的差异

测试项目 `hygkwxugskijadgfisji`：缓存初始回填 2 条，商品内容摘要仍为 `3e3c58716d53d49255a8edd55d532abf`，缓存完整性及应用维护权限检查通过。

Supabase 把 intarray 的原生工具函数归平台管理员持有，普通 postgres 不能撤销其 PUBLIC 默认权限。验证据实际所有权区分这些平台管理的原生函数和应用函数：前者必须不是 SECURITY DEFINER 或 VOLATILE；应用维护函数仍不可由客户端执行。该 schema 不得加入 Data API 暴露清单。迁移中的批量撤权在托管环境可能产生扩展函数权限警告，不能据此宣称全部扩展权限均被改写。

托管审核前后：30 次相同类型的审核从 624.558ms 降到 367.941ms（单组测量）。审核语料、字符转换、2,473 个关键词、17 条意图规则及 ACL 的摘要分别一致。这个测量只证明该步骤减少耗时，不是端到端发布 p95。

一次性写入一万条测试商品在 90 秒后被内容审核耗时触发超时，事务已全部回滚；500 条、30 秒的尝试也回滚。随后以保留触发器/外键/校验的有界批次创建测试数据。测试账号为 SQL 合成记录，商品使用历史创建时间；这是容量语料准备，不能作为 GoTrue 注册或普通账号批量发布吞吐证据。

一万零一条短描述语料已完成。30 秒/阶段、150ms 思考时间的普通匿名 HTTP 测试：单工作者所有查询通过；5 工作者时全部 107 个请求正确，但 12 词查询 p95 为 **2,148ms，未通过**原定小于 2,000ms 的标准。其他查询 p95：浏览 232ms、单词 1,841ms、三词 1,866ms、宽泛词 1,458ms、无结果 1,666ms。实测约 3.49 请求/秒，不可换算成“支持多少用户”。

随后一万零一条商品全部改为各自含独立编号的 607 字描述及双语译文，仍保留所有触发器、外键和审核。它们使用共同自然语言模板，不能等同于真实市场的独立写作语料；本地另一组含独立散列文本的复杂语料作为补充。长描述 HTTP 阶梯测试已完成并按失败率停止继续升压：

| 同时请求数 | 请求 / 正确 | 浏览 p95 | 单词 / 三词 / 宽泛词 p95 | 12 词 / 无结果 p95 | 判定 |
|---|---:|---:|---:|---:|---|
| 1 | 50 / 50 | 175ms | 550 / 563 / 493ms | 740 / 556ms | 通过 |
| 5 | 82 / 82 | 172ms | 2,269 / 2,294 / 2,032ms | 2,714 / 2,441ms | 搜索未达标 |
| 10 | 115 / 25 | 214ms | 3,265 / 3,196 / 3,204ms | 3,223 / 3,272ms | 90 个 SQLSTATE 57014 超时，停止升到 25 |

这是固定一万条规模、共同模板长描述的容量上限证据，说明当前算力/查询组合不足；本轮优化没有关闭高并发上线门槛。没有提高超时、取消 RLS 或删掉复杂查询以通过测试。

MCP 为迁移自动生成托管版本号；本地迁移文件不改名追赶远端。已观察搜索缓存托管版本 `20260914215658` 对应本地 `20260914214912`；繁简转换版本 `20260914221125` 对应本地 `20260914220849`。现有 linked CLI 指向生产且历史台账有多处既存映射差异，不能直接执行无差别 `db push`。本轮只按逐项已核对的 SQL 应用并保留对应关系。

## 运维与外部边界

回填必须在一个事务内，先取得商品写锁；2 秒内取不到锁就失败，避免旧快照覆盖并发编辑。回滚脚本恢复原搜索函数并只移除本轮缓存对象，使用 RESTRICT 保留意外依赖阻断。日后 pg_trgm、数据库字符排序或 ICU 升级，应重新验证并事务性重建缓存。

另一个把审核循环改成集合查询的原型在合成大词表上从约 2.5 秒变成 4.9 秒，已否决，未提交迁移、未应用到服务器。

实体 iPhone/iPad、微信后台站点安全限制、体验版真实账号闭环、原生签名/平台审核，以及待确认的同组织托管恢复仍按[上线门槛报告](2026-09-14-launch-gates.md)保留。没有创建付费恢复项目或跨环境复制生产图片。

证据目录：`output/playwright/search-index-20260914/`，包含原型与最终迁移的独立报告、失败记录、托管摘要、本地完整回归及设备复核。失败结果不覆盖为成功。

技术依据：[PostgreSQL intarray](https://www.postgresql.org/docs/17/intarray.html)、[PostgreSQL SQL 函数优化条件](https://wiki.postgresql.org/wiki/Inlining_of_SQL_functions)、[Supabase RLS](https://supabase.com/docs/guides/database/postgres/row-level-security)。

## 混合负载与实际修复

60 秒混合演练使用 3 个新建 GoTrue 测试账号实际登录，5 条读取循环混合匿名/普通账号，6 次聊天写入，成员/非成员 Realtime 和长轮询，100 个测试关注者与 100 个保存搜索，以及一份图片移入私有审核存储。后台处理器运行在本机、访问托管服务；不是 Vercel 上完整部署链路的峰值验收，也没有在此混合阶段加入 12 词查询。

260 个 HTTP 请求全部正确：浏览 p95 178ms，聊天写入 409ms，宽泛搜索 1,227ms，单词 1,496ms，三词 1,648ms。后台处理约 1,624ms，写入预期账号的 202 条通知，完成 1 个真实媒体移动，重试和剩余任务为零；移动前后字节摘要一致，外部账号不能读私有对象。种子准备产生的 9,501 个通知任务在演练前按本轮商品 ID 精确清除，**不能用这次结果声称处理过该大批积压**。

原始混合报告仍为失败：6 条消息均只提交一次，成员历史和长轮询读全，外部账号均读不到；但 Realtime 只投递 5 条。这个现象不能被当成 WebSocket 完整投递保证，也不能只增加等待后覆盖失败记录。

代码倒查发现，H5 只在握手、明确断线/切后台或可见事件触发时补查，正常连接悄悄漏事件时可能一直显示不完整。现已补上可取消、不重叠、失败按正常间隔重试的补查：聊天 15 秒、消息列表/通知 60 秒、报价/约见 30 秒；隐藏页面暂停，切账号和取消订阅清理定时器，降级运输接管后原定时器停止。普通事件仍立即更新。

补查还修正了一个已有 UI 问题：历史没变化或只更新已读状态时，不再每次拉到聊天底部；补回可见的未读消息时更新已读状态。77 项聊天节点回归通过；54 项 WebKit 商品/聊天浏览器回归通过，涵盖手机、iPad 横竖屏和桌面宽度。新增模拟保持握手正常但不发送消息事件的真实 SDK/前端测试，在约 15 秒后补回消息，草稿保持不变；三个屏幕尺寸均保留读旧消息的位置。**这不是实体设备或修复后的托管混合复测。**

## 存储探测判据更正

产品按新对象键上传图片，明确 `x-upsert: false`，当前没有普通用户的覆盖策略。因此原先要求“主人 PUT 覆盖成功”的探测判据不适用。主人使用实际新文件上传流程成功，普通读取返回相同字节；其他账号的新建和覆盖均被拒绝，原图字节不变。

托管 Storage 对外部账号的违规路径返回 HTTP 500 / SQLSTATE 22023。已读取实际部署的路径触发器，并在回滚事务中以普通账号核实准确拒绝信息是 `storage_boundary:invalid_owner_path`；不是靠“任意 500 都算权限通过”来推断。原探测失败文件保留。对外状态码仍不够明确，属于托管接口对自定义校验的映射限制，未放宽路径校验。

3 个临时 Auth 账号、2 份公开图片和 1 份私有图片、100 条临时保存搜索及本轮商品通知/后台任务已逐项清理。容量数据清理曾被自动审批误识别为生产删除；补充 Supabase 分支身份、精确合成记录数量和原有两条商品 ID 的只读证据，并新增严格环境数据校验后，受约束的清理获准。10,001 条合成商品和 101 个种子账号已清除，原有 2 商品、6 资料和 6 Auth 用户的内容摘要不变。三份临时凭据/会话文件也已删除。

## 新缓存恢复与安全扫描

另一次真实本机 PostgreSQL 17 ICU 逻辑导出/恢复包含新缓存和 intarray，10,001 条合成商品的内容、缓存、RLS、策略、授权、函数定义和排序保持一致；隐藏结果、越权修改和缓存写入负例通过。导入步骤 132ms，导出约 767KB，仅代表单个本地步骤，不能当成生产恢复 RTO。临时数据库和导出文件已清理。

测试环境安全扫描新增一项 `content_moderation_fold_han` 未固定 search_path 的提示。该纯 SQL 函数是 SECURITY INVOKER，仅调用显式限定的 `pg_catalog.translate` 和内建 COALESCE，没有表读取或可劫持的未限定函数/运算符；可执行权限仍仅 postgres，恶意同名函数查找路径测试通过。去掉配置切换是内联生效的必要条件，因此保留此明确分析的提示，不能报告“零告警”。扫描还列出已有受控 SECURITY DEFINER RPC、刻意不开放客户端策略的后台表，以及测试环境未开启泄漏密码检查；不把所有提示统一视为漏洞或全部消除。

[Supabase 函数查找路径提示说明](https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable)。

后台现已核实 `staging-smoke` 使用 **Micro / t3.micro / 1GB**，所有读写共用主库、没有只读副本。已准备但未确认临时 Large 方案：$0.1517/小时，页面月度等价从 $9.68 变为 $109.22，并提示重启及可能较长停机。已请求最多 2 小时、总计不超过 $1 的临时测试授权，结束后回到 Micro；未点击最终确认，生产算力未改。该对照能判断资源限制的影响，不能预先保证升配就能解决查询问题。[计费和规格说明](https://supabase.com/docs/guides/platform/compute-and-disk)、[按小时计算规则](https://supabase.com/docs/guides/platform/manage-your-usage/compute)。


## 托管回滚与发布边界

已在同一测试分支实际撤销缓存/扩展/触发器并恢复原搜索、繁简转换函数，再应用两份未修改的迁移。回滚时三个原函数定义摘要完全恢复，10,003 条商品保持；回滚前、旧函数状态和重新应用后的单词/12 词完整有序 JSON 摘要均一致。重新应用后缓存完整性与权限检查通过。回滚/重放只在测试分支执行。

测试账本新增 `20260914231928`（联合回滚演练）、`20260914232140`（缓存重放）、`20260914232156`（繁简转换重放）；它们是演练记录，不是新的本地 migration 文件，也不应整分支合并进生产。

生产只读基线为 PostgreSQL 17.6、14 条商品，旧 v1/v2 查询摘要分别为 `6ef8936a58d9cd44c816767b28f61d56`、`c1ade4fa3b408d1ef7265564914e00bc`，没有 search_private/intarray。本次实际应用生产缓存被自动审批拒绝，理由是需要对扩展、触发器、回填、表锁及服务影响单独明确授权；已向用户请求两份具体迁移的授权，第一项未执行，第二项也未尝试。**生产数据库优化尚未生效。**

GitHub/Vercel 工作流只发布前端/API，不部署数据库迁移；Supabase 主分支 `git_branch` 为空。提交候选 SQL 供审查不等于应用它。聊天修复继续独立发布，生产数据库保持等待授权。


前端相关本地验收已完成，提交及 Vercel/CI 回执待下方发布记录更新。付费恢复、临时算力对照、生产数据库迁移的三个确认项均未收到回答；没有用等待时间代替授权。
