# 手机与平板键盘场景补测

起始版本：`45e15e56be2ca291b94ae518d27325d53a71e038`，本轮开始时本地、main 和正式站点清单一致。
本轮按照浏览、价格筛选、发帖、关联商品、评论的用户路径继续检查。
最终提交及正式部署回执保存在本地 `output/playwright/mobile-followup-20260911/RELEASE_RECEIPT.md`。

## 已修复的问题

| 场景 | 修复前证据 | 修复后行为 |
| --- | --- | --- |
| 手机价格筛选 | WebKit iPhone 配置、布局高度664px，模拟键盘后的可见底边360px；滚到底后 Apply 仍位于519–571px | 面板限制到16–360px，Apply位于214–266px；触屏提交成功 |
| 筛选可读性 | 截图中商品标题透过面板，滚动内容又穿过透明标题栏 | 面板和标题栏使用随主题变化的不透明底色；关闭按钮扩大到44px |
| 广场发帖工具栏 | 相同模拟可见底边360px时，工具栏仍在616–664px | 编辑器整体缩到可见区域，工具栏位于312–360px；关联商品可以打开/关闭，正文保留 |
| 帖子详情评论 | 相同条件下评论输入区域位于606–664px | 页面滚动区和评论输入栏整体适配可见区域 |
| 广场评论与关联商品弹层 | 代码检查发现沿用只依赖vh/dvh的高度，没有接入视觉视口 | 一并限制在可见区域；新增触屏回归验证横竖屏下关闭按钮和回复入口可达 |

共享视口逻辑额外提供可见高度。键盘引起视口平移时同时约束上下边界；浏览器已缩小布局时不重复扣高度；放大页面时不把缩放当成键盘。小程序继续使用原有原生键盘事件。

## 本地验证

- WebKit 商品、发布和聊天回归：63项通过，包括发布失败恢复、详情布局、聊天、筛选提交和取消恢复。
- 广场、评论、关联商品弹层与读取失败恢复：19项通过。
- 新增8项回归覆盖键盘遮挡及视口平移。广场覆盖320、390、834、1194px；商品筛选覆盖360、390px和iPad横竖屏。
- 独立浏览器操作：iPhone配置、iPad Pro配置横竖屏；320px深色筛选没有横向溢出；触屏应用筛选、关联商品关闭后草稿保留均通过。
- 额外检查：布局缩高和模拟双倍缩放下，筛选面板保持相同252px高度，没有重复上移。
- 新增广场测试最初误用input选择器查找textarea，导致超时；纠正为有名称的文本框后，19项完整重跑通过。该超时未算作产品故障或通过证据。
- 首次发布门禁要求旧的透明面板CSS类名，移除透明效果后触发了这个静态断言；同步类名，保留原有焦点、角色和键盘行为断言，再执行完整门禁。

全部本地账号、帖子、消息和商品写入由隔离夹具接管，没有向真实用户发送消息或发布测试商品。
截图、测量和测试日志保存在上述本地目录。自动发布检查与正式站点验收分开记录，不把本地通过当作上线凭证。

## 仍需真实设备的证据

这里的键盘测试通过改变VisualViewport属性触发真实页面重排，不能证明iOS中文输入法、真实软键盘动画、Safari工具栏、相册/HEIC、手势手感或切后台恢复全部正常。
本轮也没有增加生产高并发压测证据。无新增已知发布阻塞，仅表示本轮复现的缺陷已修复并通过对应验证，不表示整个产品绝无缺陷。
