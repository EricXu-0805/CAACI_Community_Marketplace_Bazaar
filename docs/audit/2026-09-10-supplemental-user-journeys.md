# 2026-09-10 用户操作补测与修复

起点：本地、main 和生产 manifest 均为 `cd78e47806a31e4c06153a58cc003228593b7a8f`。
本轮沿实际操作顺序、中文输入、失败恢复、图片预览和并发写入补测。
最终代码 SHA、CI 与部署结果见本轮本地回执 `output/playwright/supplement-20260910/RELEASE_RECEIPT.md`；本文不把候选视为已部署。

## 新发现并修复

| 问题 | 修复及证据 |
| --- | --- |
| 中文选词回车意外执行搜索、发送帖子评论 | uni H5 在 keyup 无条件发出 confirm。全局输入桥记录 composition 与对应 Enter 键对，保留选词，下一次主动 Enter 正常提交。旧版本两条浏览器案例失败，修复后通过。真实系统输入法尚需设备验证。 |
| 帖子详情发送较慢时，完成旧评论会清空新草稿，回复名字也读取了新上下文 | 保存提交时的正文和回复对象；只清除仍属于这次提交的输入。延迟响应期间输入新内容的失败前/修复后证据已保留。 |
| 广场评论失败会恢复旧草稿；输入框可能仍显示新内容，下一次实际发送的却是旧内容 | 只在用户没有输入新草稿或选择新回复时恢复；回归同时检查实际 POST 正文，不仅检查输入框显示。 |
| 评论面板关闭、重开时，旧请求还能改写新面板 | 每次打开/关闭都有独立代次；过期读取、提交和 finally 不能更新新面板。延迟提交后关闭、重开并再次提交的浏览器案例通过。 |
| 商品大图预览允许 Tab 进入背景，关闭按钮没有可读名称 | 在生产商品页面实际复现焦点泄漏；为原有预览表面添加 dialog 语义、关闭名称、焦点圈定、背景 inert 与返回焦点。复测 Esc、Enter、Tab、Shift+Tab、路由切换，覆盖 1440/820/390px。Esc 原先已能关闭，不作为新修复。 |
| 同一用户并行写入能突破限额和短窗口去重 | 旧函数先计数再插入，没有序列化。临时 PostgreSQL 中，剩余一个名额时 8 个请求全部成功，五种表均复现。新增私有 SECURITY INVOKER 触发器按表和用户取得事务锁，之后执行原有限额函数；修复后各组只接受一个，另外七个得到明确限额错误。 |

## 验证层次

- 浏览器：WebKit 的商品/聊天/弹窗联合回归 51 条通过；最终新增 8 条再次通过。Chromium 的新增及商品/聊天回归 34 条通过。数据、账号、网络失败与键盘事件均为合成夹具。
- 本地边界：1382 通过，0 失败，1 条既有本地 Vercel 产物清单检查跳过。
- 本地数据库：11 条断言通过，涵盖五种额度、四种重复提交、不同用户/不同表不互阻、事务回滚释放锁和普通角色触发器执行。只证明竞态修复，不证明生产容量。
- 测试数据库 `hygkwxugskijadgfisji`：迁移已应用；五个触发器启用；函数不提权且普通角色不能直接调用；应用前后逐项比较 RLS、表/列权限、策略、五个原有限额函数定义，完全一致。普通账号的商品插入触发一把事务锁，随后整体回滚，无测试商品留存。
- 正式数据库 `lfhvgprfphyfvhidegum`：自动审批连续两次超时，复查新函数不存在；本记录写入时仍待明确确认。不要把仓库迁移或测试环境结果当成正式环境已应用。
- 测试环境安全顾问：已有私有/后台表无策略 INFO、19 个可供登录用户调用的 SECURITY DEFINER WARN，以及泄露密码保护 WARN；本次新增函数不属于这些告警。未为了清空告警擅自开放后台表或移除业务权限。

## 数据库变更与回滚

源文件：`supabase/migrations/20260910211948_serialize_public_write_limits.sql`。
验证：`supabase/_ops/serialize_public_write_limits_verify.sql`。
紧急回滚：`supabase/_ops/serialize_public_write_limits_rollback.sql`，会恢复旧并发竞态，只在确认运行回归时使用。
保持原有限额、行权限、列权限和审核逻辑；触发顺序在载荷/账号校验和审核之后、计数之前。
已核验两个环境使用 READ COMMITTED，普通登录角色有 8 秒语句超时。
实现依据：[PostgreSQL 事务锁](https://www.postgresql.org/docs/current/explicit-locking.html#ADVISORY-LOCKS)、[事务隔离](https://www.postgresql.org/docs/current/transaction-iso.html#XACT-READ-COMMITTED)。

## 仍不能替代的上线验收

真实 iPhone/iPad 的系统中文输入法、软键盘、图片手势和后台恢复尚未补成设备证据。本机没有可用 iOS 模拟器；桌面浏览器尺寸与合成输入事件不能勾选真机通过。
双用户真实设备互相收发、实际弱网/切后台、生产级容量与持续负载仍需对应环境证据；本轮没有向生产发压测流量或测试消息。
