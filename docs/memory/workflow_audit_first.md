---
name: Workflow — audit-first, atomic commits, 5-section final report
description: Eric's required workflow: audit → spec → build → review → push → verify; atomic commits "修一个验收一个"; final report must include diff/builds/static reasoning/git state/anomalies
type: feedback
---

Workflow preference: audit-first → spec → build → review → push → verify. Atomic commits, "修一个验收一个" (fix one, verify one). Audit phase mandatory before build. Spec must include final report with 5 sections (diff + builds + static reasoning + git state + anomalies).
