{
  "name": "caaci-community-marketplace-bazaar",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "cd app && node --test ../api/*.test.mjs ../scripts/*.test.mjs smoke/*.test.mjs",
    "verify:migrations": "cd supabase/migrations && shasum -a 256 -c manifest.sha256"
  },
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": "22.x"
  }
}
